# FinTrace Design Lab - Implementation Plan & State

This document is the standalone source of truth for the FinTrace design-lab repo: why it exists, what has been built across six rounds, exactly what is on every page right now, the binding technical rules, and what happens next. A new contributor (human or agent) should be able to work from this file alone.

## Why (context and goal)

FinTrace is the new brand name for the application formerly called Statement Analysis (`/Users/sacino/fintrace`): a forensic financial analysis service that converts bulk PDF bank statements (any bank, any order, scanned paper included, no pre-sorting) into a single structured, source-traceable Excel ledger (file name, person, date, financial year, description, debit/credit, amount, category), auto-categorises transactions (Woolworths -> groceries), flags suspicious activity (cash-withdrawal patterns, gambling/crypto, transfers between related accounts, cross-currency matches such as rupee-to-AUD via Wise), and produces a written findings report with every finding traceable to its exact source PDF page (human-verifiable, no hallucination risk). Proven value: a ~50 hour manual lawyer job completed in ~10, findings matching the lawyer's independent analysis; real matters run to thousands of pages, ~50 accounts, 15 years.

Buyers: lawyers in document-heavy financial disputes (family law is the proven wedge), government legal bodies (Public Trustee / Dept of Justice), misappropriation and elder financial abuse matters, forensic accountants, insolvency practitioners. Trust and credibility are the biggest barrier (LexisNexis-grade gravitas benchmark). Positioned as a service, not software (flat cost + per-page pricing, engaged per matter). All public contact actions route to `/contact/`; CTA style "Request a matter assessment".

Full brand brief: `/Users/sacino/fintrace/documents/reference/brand_naming_background.md`

Goal of this repo (`/Users/sacino/fintrace-root`): serve the selected Engine Network design as the public FinTrace homepage while retaining the comparison designs in an unlinked internal design lab.

## Production architecture (selected 17 July 2026)

- `https://fintrace.com.au/` and `/` use the Engine Network flagship through the shared `src/app/engine-network/EngineNetworkPage.tsx` Server Component. `/about/`, `/engagement/`, `/contact/` and the branded root 404 extend the same production visual system.
- `/internal-design/` is the neutral comparison gallery. It and all 11 candidate routes remain public but unlinked from the production homepage, excluded from the sitemap, and marked `noindex, nofollow, nocache`; this is not access control.
- `Culpable/fintrace-root` publishes the `out/` static export from `main` through the native GitHub Pages workflow in `.github/workflows/deploy.yml`.
- `src/app/sitemap.ts` lists the four indexable production pages: `/`, `/about/`, `/engagement/` and `/contact/`. `src/app/robots.ts` permits crawling so internal-route metadata can be observed.
- Cloudflare authoritatively serves the required GitHub Pages DNS set: four apex A records, `www` CNAME `culpable.github.io`, and the `Culpable` account-verification TXT record. Unrelated records are outside this deployment's scope. GitHub reports the account domain verified and HTTPS enforced; Let’s Encrypt certificates cover both hosts, the apex serves the Pages artefact, HTTP upgrades to HTTPS, and `www` redirects to the apex.

## Decisions locked in via blindspot pass (user-approved, round one)

1. Stack: Next.js 16 + React 19 + Tailwind CSS v4 + TypeScript, App Router, static export (`output: 'export'`, `images.unoptimized`, `trailingSlash`), dev port 3004 (unique in workspace; 3000-3003, 4021, 4022, 4031, 5001 taken). Scaffolding mirrors `/Users/sacino/bulma-root/demo` (modern variant of the Embeddings pattern).
2. Scope: each design is a FULL self-contained homepage (hero, product narrative/how-it-works, capabilities/findings, proof/credibility, who-it's-for, CTA, footer) with its own typography, palette, and motion system. The neutral gallery now lives at `/internal-design/`; `/` is production Engine Network.
3. Copy: shared core facts from the brand brief, headlines/tone tuned per design concept. Each design gets its own typographic FinTrace logotype. British English, curly apostrophes (’), no emoji.
4. Animation budget: bespoke CSS/SVG/Canvas + scroll-driven animation throughout; WebGL/three.js permitted for 1-2 showpiece designs.
5. Count: exactly 6 designs, full aesthetic range (conservative legal-grade trust through bold dark forensic/investigative).

## Technical rules (binding for all agents)

- Static export only: no server runtime, no API routes, no next/image optimisation. Fonts via `next/font/google` (self-hosted at build); never Inter/Roboto/Arial/system fonts. No external/network assets - all visuals are CSS, inline SVG, canvas, or generated WebGL textures.
- Isolation: each route owns its complete visual system in its folder - `page.tsx` (server component + metadata), scoped `<route>.css` (ALL rules nested under a `.dsn-<route>` wrapper class - this is what prevents cross-page style bleed during client-side navigation), `fonts.ts`, colocated `'use client'` components. `globals.css`/`layout.tsx` stay minimal/neutral. `three` is dynamically imported inside WebGL routes only (the /engine family).
- `@keyframes` names are DOCUMENT-GLOBAL (not scoped by the `.dsn-*` wrapper): any keyframes duplicated across route stylesheets silently collide under client-side navigation (last loaded wins). Every route must prefix its keyframe names uniquely (round three uses `ecm-`/`ecmr-`/`eclm-`/`ecmnet-`/`ecmf-` for the shared match diagram; round four's ledger-table copy on /engine-network uses `engnet-lt-*` so it never collides with engine-ledger's `eng-lt-*` originals).
- Animations: transform/opacity wherever possible; IntersectionObserver scroll triggers; passive listeners; rAF/WebGL loops pause when tab hidden or element offscreen; canvas DPR capped at 2; rAF clock deltas clamped to [0, 0.05] (Chrome's first rAF timestamp can predate the start clock - unclamped, this crashed the original /trace hero via a negative array index); full three.js disposal on unmount; WebGL renderer construction wrapped in try/catch so a missing GL context leaves the designed static fallback instead of crashing the React tree. NEVER add `prefers-reduced-motion` gates (workspace rule).
- WebGL hero FRAMING BUDGET (learned in round three - three of five reworked heroes shipped off-frame on first pass): with the engine-family camera (fov 34°, lookAt(0, 0.1, 0), desktop z=10.5/aspect 1.6, mobile z=14/aspect 0.433), the original usable stage-local x limit at depth z is ≈ (10.5 − z) × 0.489 − 1.1 (stage shift) − 0.95 (camera drift 0.35 + pointer parallax 0.6) on desktop, and ≈ (14 − z) × 0.1324 − stage shift − 0.35 on mobile. `/engine-network` is the flagship exception from round five: wide `stage.position.x = 0.6875 × aspect`, pinning the gate near 60.7%; rest-state width right of the gate is `2.5225 × aspect − 0.35`, while the max-sway review reserves the additional 0.6 pointer allowance; and the constellation fit is `clamp((2.5225a − 0.35) / 3.686, 0, 1.3)`. Rings and plates keep the full group fit, vertical slot positions compress smoothly to a 0.95 factor at the 1.3 cap (`verticalPositionFit = max(0.95, 1 − (fit − 1) / 6)`), z slots use the inverse ratio `1 / fit` to retain authored world depth, and node cores use local scale compensation `1 / sqrt(fit)` so their projected growth is only `sqrt(fit)`. Compact remains at stage x −0.7. Any scene layout change requires headed screenshots because frustum maths sets the budget, only the browser proves it, and depth foreshortening can create projected crowding that is absent in slot space. The network matrix is 1440x900, 1998x750, 2560x1080, 3425x1245, 1024x768, 900x1080 and 390x900; ledger retains its round-4.1 matrix.
- better-ui skill compliance (user-mandated from round two onward, skill at `~/.claude/skills/better-ui/`): active press scale 0.96 with explicit transition-property lists (never `transition: all`), >=44px hit areas (pseudo-element extensions), concentric border radii on nested rounded pairs, layered low-opacity shadows for elevation (hairline borders kept where they are the design language / dividers), interruptible hover/press transitions, visible focus states.
- Responsive: clean at 1440x900 and 390x900, zero horizontal overflow.
- Parallel workers must not run `npm run dev`/`npm run build`/git (shared `.next` conflicts) - the coordinating agent validates centrally. Workers MAY verify their own route visually via `dev-browser` against the already-running dev server, using a unique page name per worker to avoid collisions.
- Validation gate per round: `npm run lint` (zero errors), `npm run build` (static export completes), browser verification of every changed page at 1440x900 + 390x900 (console errors, page errors, horizontal overflow, animation rendering), plus headed-browser real-GPU verification for WebGL scenes. Any `/engine-network` hero change also requires 1998x750, 2560x1080, 1024x768 and 900x1080, plus the live-resize, fallback and lifecycle checks recorded above.

## Commands and environment

- `npm run dev` → http://localhost:3004 (assume it may already be running; check before starting another instance).
- `npm run build` → static export into `out/`. `npm run lint` → ESLint, must be zero errors.
- Browser verification is done with the `dev-browser` CLI (headed Chromium, real GPU, Playwright Page API in a sandboxed script runtime; screenshots land in `~/.dev-browser/tmp/`).
- No automated tests exist; validation is lint + build + browser. GitHub Pages deploys from `main` through `.github/workflows/deploy.yml`. The public Git remote is `git@github.com:Culpable/fintrace-root.git`.
- Repo-root `AGENTS.md` carries the binding content/isolation/animation/testing rules and mirrors the rules above.

---

## History: six completed rounds

### ~~Round one: six designs~~ ✅ COMPLETED

All built by six parallel fork agents, validated (lint/build/browser both viewports/headed WebGL), committed (`6f3ad31` scaffold, `1b965a4` designs). Defects found and fixed during verification: /trace negative-dt first-frame crash (clamped); /engine blank page when WebGL unavailable (try/catch + persistent fallback); three react-hooks lint errors (engine/Reveal ref-in-render → React 19 callback ref; engine/Scene ref write in render → effect; exhibit/HeroRegister closure mutation → loop); ledger mobile table masthead cramping (stacked). Favicon `src/app/icon.svg` added.

**User feedback on round one (verbatim intent):** `/engine/` is the BEST overall - design, font, colour scheme; most unique in a non-tacky way - BUT the "dust" coming out of the gate doesn't make sense (paper going in is loved; the point-burst output reads as meaningless). `/trace/`: likes the diagrams/drawings - the hero network background and the "CROSS-CURRENCY MATCH - RECONSTRUCTED" set-piece - but NOT the font or colour scheme. `/ledger/`: decent; font pretty good, logo decent, likes the table though it "looks like it has some errors in it" (diagnosis: the arithmetic was correct; the flagged-row red rule rendered as broken fragments - an SVG `preserveAspectRatio="none"` + dash-animation artefact). Does NOT like white + black. Direction set: keep `/engine/` AS-IS as the reference; build VARIATIONS of it. All designs must align with the better-ui skill.

### ~~Round two: five Evidence Engine variations~~ ✅ COMPLETED (V4/V5 superseded in round three)

Five variants built by parallel forks from the /engine base (all replace the post-gate "dust" with structured output, keep the document stream + gate, and carry the better-ui pass), committed (`bd8cf99`): V1 `/engine-refined` (record-slab lattice control), V2 `/engine-trace` (evidence-card arc + `#tracing` section), V3 `/engine-ledger` (cell-mosaic sheet + `#ledger` reconciling table), V4 `/engine-serif` (Fraunces typography A/B - since REMOVED), V5 `/engine-light` (parchment inversion - since REMOVED).

**User feedback on round two (verbatim intent):**

- Dark mode only; no light mode.
- Keep the font as per the original `/engine/` (Bricolage Grotesque + Fragment Mono) - no font changes.
- `/engine-refined/` has the right idea but is "way too busy" - a lot less going on needed (too many items).
- `/engine-ledger/` is a good idea but unclear what's going on; the hero RHS must look more like a spreadsheet.
- `/engine-trace/` has the right idea but looks "off": the card connections read as "a string with cards connected to it" - the connections need rework.
- Serif and Light: redo into two NEW dark variations on the original font, and use the opportunity to redesign the hero RHS as something "Trace"-related - closer to the `/trace/` trace diagrams.
- In ADDITION to everything already on the engine variation pages, every variation must include a diagram like /trace's "CROSS-CURRENCY MATCH - RECONSTRUCTED" - restyled to the engine palette, with the overlap fixed (in /trace the MATCHED badge overlapped the travelling A$28,004 chip at landing, and the right-hand station labels clipped the viewBox edge).

### ~~Round three: feedback pass on the five variations~~ ✅ COMPLETED

Executed by four parallel fork agents + coordinator (refined/ledger/trace reworked in place; network/flow built fresh from the committed engine-refined state; engine-serif and engine-light deleted). Committed as `955fb25`. All validated: lint zero errors, static build green (14 routes), browser verification of all changed pages at 1440x900 + 390x900 with zero console errors, zero page errors, zero horizontal overflow, all five WebGL scenes rendering on a real GPU (headed Chromium via dev-browser).

Defects found and fixed during round-three central verification:

- FRAMING (the big one): first-pass layouts for ledger/trace/network/flow placed structures at stage-local x 4.8-10.7 - almost entirely outside the camera frustum (desktop limit ≈3.1 at z=0; mobile ≈1.5). Diagnosed centrally from screenshots + frustum maths, fixed by the same agents against explicit per-depth budgets, then re-verified visually at both viewports. The budget formula now lives in Technical rules above.
- Mobile occlusion: first mobile passes on network and flow hid the CASH ATM node/label behind the opaque gold CTA button; fixed with a +0.35..+0.5 y bias on the mobile layouts.
- Thread crossing: network's WISE→BTC edge crossed ANZ→CBA in the compact layout; BTC re-parented to CBA.
- Instanced-mesh over-allocation on refined: the four slab meshes allocated `ceil(SLAB_COUNT/4)` instances uniformly, which at 9 slabs would render never-written identity-matrix instances as frozen slabs at the gate centre; each mesh now allocates exactly what the round-robin writes.

### ~~Round four: flagship consolidation on /engine-network~~ ✅ COMPLETED (including the 4.1 follow-up)

**User feedback on round four (verbatim intent):** "Much nicer in general." (1–2) /engine-ledger hero: the Excel spreadsheet is at way too much of an angle, making it hard to read — it should be basically FRONT-ON, with effects when you hover over it. (3) /engine-network's hero image is the favourite so far. (4–5) But some of the thread lines overlay the node descriptors — fix so lines never overlay the labels (clearly possible, it was already done on at least one node); also space the nodes out a little more, they're too cramped. (6–15) "See the first attached screenshot" — the screenshot arrived in a follow-up message: it showed the network's ORIGINAL spacing/overlay problems (the substance of items 4–5), and the follow-up asked for two refinements — verify/avoid the issue on smaller screen sizes, and separate the top two nodes more (NAB to the right). All addressed in 4.1 below. Direction set: /engine-network is the DEFAULT page going forward (the best one); all below-the-fold edits apply only there; gather the images/diagrams built on the other engine pages (the ledger table from /engine-ledger, the network/trace diagram from /engine-trace) onto /engine-network below the fold, tied into one cohesive FinTrace story.

**What was implemented (all validated: lint zero errors, static build green, dev-browser at 1440x900 + 390x900 on both changed routes, zero console/page errors, zero horizontal overflow, WebGL on real GPU, hover exercised end to end):**

- `/engine-network` hero (Scene.tsx): nodes spread wider in x, y AND z (hub 2.35/0.05/−0.2; WISE 1.7/1.5/−0.55; CBA 3.3/1.15/−0.9; CASH 2.6/−1.6/0; AMEX 1.45/−1.3/−0.35; HDFC 2.5/2.4/−0.95; NAB 3.15/2.15/−1.2; BTC 3.45/−0.55/−1.1 after the post-4.1 spacing adjustment), and every node gained a per-node `labelOffset` placing its label plate on its THREAD-FREE side (ANZ/WISE/CBA beside the ring at ±0.73, HDFC/NAB above at +0.38, CASH/AMEX/BTC below at −0.38; mobile equivalents likewise). Root cause of the overlays: labels were uniformly pinned BELOW nodes, so any thread leaving/arriving vertically crossed them. Every label rect was checked against every thread segment (rim-inset endpoints) in both layouts before the browser pass confirmed clear air.
- `/engine-ledger` hero (Scene.tsx): the spreadsheet now stands FRONT-ON (resting yaw −0.04 rad, was +1.0), rebuilt on a 1120x456 texture with ~50% larger type on a 3.1-unit plane (desktop centre 1.66/1.08/−0.45 — riding HIGH so the gold "engine." h1 never crosses it; the first front-on pass at y 0.12 collided with the headline and was caught by screenshot). Mobile: scale 0.72, centre 0.9/2.75/−0.2 — parked in the clear band between the fixed header and the kicker. HOVER EFFECTS via window-pointermove raycasting (single-quad intersect): the sheet tips toward the pointer like a held card (±0.05 rad), lifts 0.12 toward the camera, its frame brightens, an additive gold aura breathes in behind it, the row under the cursor takes a gold wash + left tick with lifted ink — and the sheet's own clock HOLDS at the brink of fade-out while hovered so the finished ledger never dissolves mid-inspection (canvas still redraws only on state changes, never per frame).
- `/engine-network` below the fold — THE CONSOLIDATION: `LedgerPlate.tsx` (copied from engine-ledger) and `TraceDiagram.tsx` (copied from engine-trace) now live on the network page beside the existing CurrencyMatch, sequenced as one story between `#process` and `#capabilities`: `#ledger` (kicker "The evidence, structured" — every line entered/sourced), `#tracing` (kicker "The evidence, connected" — fifty accounts, one thread), `#match` (kicker "The evidence, matched" — same funds, two currencies), with linking ledes carrying the arc pages → ledger → network → finding. CSS blocks ported into engine-network.css rescoped to `.dsn-engine-network`, ledger-table keyframes re-prefixed `engnet-lt-*`. Header nav is now Process / Ledger / Tracing / Proof.

**Round 4.1 follow-up (the "items 6–15" screenshots arrived — they showed the network's original spacing/overlay problems, plus two refinements):**

- Crown separation: NAB moved right and deeper (3.4, 2.0, −1.5), HDFC left and higher (2.35, 2.55, −0.9), so NAB's plate clears HDFC's ring with real air even under maximum camera parallax (they previously touched in projection despite a clean 2D-check — depth foreshortening pulls deeper nodes toward the vanishing centre; check crowding in PROJECTED space, not slot space).
- Smaller screens (network AND ledger scenes): layout selection is now by ASPECT, not width alone — `compact = width < 768 || aspect < 1.2` — so portrait tablets and narrow split-screen windows (768x1024, 900x1080) get the tall five-node/high-sheet configuration instead of a clipped wide one. Between aspect 1.2 and the designed 1.6, a continuous fit scales the whole constellation (`net.scale`, spacing and plates alike, so nothing re-crowds) / shrinks-and-shifts the sheet toward the gate, live on resize (crossing the wide↔compact threshold still needs a remount). The compact CASH ATM placement is aspect-conditional: phones (aspect < 0.6) keep it low with the label below (clear of the CTA); taller windows lift it with the label hung to its RIGHT (the kicker + h1 own the low band there; the hub thread arrives steeply from above so the right side is thread-free in both variants).
- Verified matrix (headed GPU, zero errors/overflow): network at 1440x900, 1024x768, 900x1080, 390x900; ledger at 1440x900, 1024x768, 390x900.

**Post-4.1 spacing adjustment:** moved the wide-layout BTC WALLET node 0.25 stage units right, from x 3.2 to x 3.45, while preserving y −0.55, z −1.1, its below-node label, and the existing CBA → BTC thread. This creates more visible separation from the lower central branch without changing compact layouts, where BTC is intentionally omitted.

### ~~Round five: /engine-network wide-aspect rebalance and delight pass~~ ✅ COMPLETED

**Why this round happened:** the user reviewed the flagship in a 1998x750 logical browser window (aspect ≈2.66). The fixed +1.1 stage shift projected the gate near 56%, the constellation stopped around 73% of the viewport because its fit capped at 1, and the centred hero container moved the headline to x 447.5 so the gold `engine.` word met the gate halo. Documents also crossed behind the kicker at several animation phases.

**What shipped:** implementation is isolated to `src/app/engine-network/Scene.tsx`, `Hero.tsx`, `engine-network.css` and the two plan documents. The post-change sweep also wrote its XML evidence under `documents/todo/bugs/codex/`.

- Framing: wide stage shift now follows `0.6875 × aspect`; the constellation fit is `clamp((2.5225a − 0.35) / 3.686, 0, 1.3)` and reapplies with the stage shift in the existing ResizeObserver. The group scales around the lifted gate so births remain aligned. Browser tuning retained uniform 1.3 ring/plate growth but compressed wide-screen vertical slot positions smoothly to 0.95 at the cap, preventing HDFC/NAB from entering the fixed header. Label textures moved from 256x64 to 512x128.
- Headline and contrast: the hero container no longer recentres past the canonical composition. Its content-width calculation preserves the exact 1440x900 x position (168.5 px with Chromium's classic scrollbar) and caps the wide left edge at 176 px. This deliberately uses `50% − 37rem` instead of the draft plan's `100vw` formula, which would have shifted the canonical baseline by 7.5 px. A third radial scrim pocket now protects the kicker, h1 and lede.
- Gate and flow: the gate sits at y 0.4; radii are 1.45/1.31; halo is scale 5.5 at local y +0.55 with opacity `0.36 + sin(t × 1.6) × 0.08`; and the core is scale 2 at local y +0.15. Documents and node births target the lifted centre. The document funnel begins at progress 0.72. Paper uses `#f6edd8 → #e5d5b2` and each instanced page warms towards RGB (1.15, 1.02, 0.75) near the gate.
- Delight systems: document wraps feed a clamped, decaying flash into the existing core; two pooled three-sprite mote trains lead node births; one short dash travels parent-to-child along every settled thread, with the CASH dash faster/brighter and a pre-exit fade; the CASH ring lands a one-shot flare and a left-anchored underline wipe; and hero `engine.` uses route-unique `engnet-sheen` over 3.9 seconds. No new render-loop allocations, ghost network, exit drift, gate yaw, scan plane or reduced-motion gate was introduced.
- Fallback: the wide SVG echo moved from cy 430 to 395, its radii from 150/132 to 138/121, its flow line to y 395, and the fallback radial centre from 48% to 44%. A second five-node 900x1080 SVG now mirrors the compact camera. The same `width < 768 || aspect < 1.2` CSS contract selects it, and `calc(50% - 8.2svh)` keeps the fallback glow on the compact gate at both 390x900 and 900x1080. This post-audit correction removes the previous 259-310 px cross-fade jump on tall viewports.
- Post-change WebGL hardening: the independent sweep found that Three.js could retain the continuously rewritten document mesh's first computed frustum bound and that its instanced matrix/colour buffers needed their own disposal event. The document mesh now disables frustum culling and calls `documents.dispose()` before geometry, material and renderer teardown. The sweep's three findings are preserved in two worker reports and `combined_bug_sweep_20260716_c9e4a1f7.xml`; every finding was fixed and revalidated.

**Validation:** `npm run lint` exited 0; `npm run build` compiled, typechecked and statically generated 15 pages; headed dev-browser checks passed at 1440x900, 1998x750, 2560x1080, 1024x768, 900x1080 and 390x900 with zero console/page errors and zero horizontal overflow. At 1998x750 the gate measured ≈60.4%, the BTC plate reached x 1703 (85.24%), the h1 started at x 176, and the headline/ring/halo remained geometrically separate across three phases. A live 1300→2600 resize retained the same hero canvas and zero overflow. WebGL ran through ANGLE Metal on an Apple M2 Max at about 100 measured fps. Offscreen and controlled hidden states reduced scene rAF callbacks to zero and resumed cleanly. Three garbage-collected mount/unmount cycles held JS heap between 11.48 MB and 11.65 MB. The script-frozen first paint verified the updated wide fallback. After the audit fixes, script-frozen and live captures at 390x900 and 900x1080 placed the fallback and WebGL gate in the same projected position; both compact routes retained one canvas, zero overflow and zero errors. Three further gallery-to-route cycles retained one canvas and completed without browser errors. Lint, build and `git diff --check` were rerun after those fixes and passed.

### ~~Round 5.1: /engine-network node crispness follow-up~~ ✅ COMPLETED

**Why this follow-up happened:** the user confirmed the wide rebalance but reported that node glows looked fuzzier once the constellation reached its 1.3 fit cap. The review isolated three actionable causes: the 64x64 dot and 256x256 halo textures were being magnified, node cores inherited the full group growth, and z slots scaled deeper into fog and towards the projected vanishing centre.

**What changed:** `src/app/engine-network/Scene.tsx` now bakes the shared dot texture at 128x128 and the gate glow at 512x512 with unchanged gradient stops. `applyNetFit` also publishes two allocation-free ratios: `netCoreScaleRatio = 1 / sqrt(fit)` applies only to node core sprites, while `netDepthPositionRatio = 1 / fit` applies to node and birth-mote z targets. Rings, label plates, thread geometry, x growth, y compression, cycle timing and the 1.3 fit cap remain unchanged. The cap was deliberately retained after 3425x1245 verification showed the constellation still occupied the accepted 84-89% phase range without clipping.

**Validation:** matched before/after node crops were captured at 1440x900, 1998x750 and 3425x1245. The after captures show smaller, more defined hot centres, stronger crown/right-edge contrast and increased HDFC/NAB projected clear air. Headed ANGLE Metal checks passed at 1440x900, 1998x750, 2560x1080, 3425x1245, 1024x768, 900x1080 and 390x900 with one canvas, visible focus, zero console/page errors and zero horizontal overflow. A live 1300x900→3425x1245 resize retained the same canvas. In an isolated 1440x900 tab WebGL rendered 201 frames in 2 seconds, dropped to zero frames while offscreen, then rendered 87 frames in the first second after resuming. A temporary forced renderer failure left the static fallback at opacity 1 with no canvas, no error and no overflow. `npm run lint`, `npm run build` and `git diff --check` passed.

### ~~Round 5.2: /engine-network label sampling follow-up~~ ✅ COMPLETED

**Why this follow-up happened:** after the node-core correction, the user identified a separate softness concentrated on the top and bottom edges of the account-label glyphs. A headed 3425x1245 reproduction showed that the nearby torus geometry remained sharper than the plate text, ruling out CSS blur, whole-scene post-processing and display scaling as the primary cause.

**What changed:** the 512x128 label bake and 48 px Canvas font remain unchanged. `makeLabelTexture` now disables mipmap generation and uses direct `THREE.LinearFilter` minification/magnification. The default trilinear mipmap path was averaging the glyph into progressively smaller prefiltered levels before the 0.9x0.225 plane reached its approximately 13-18 px screen height, applying a second blur pass on top of Canvas antialiasing. Direct linear sampling retains the Canvas edge smoothing while preserving materially clearer horizontal glyph edges and plate borders.

**Validation:** matched before/after crops at 3425x1245 showed visibly firmer glyph caps, baselines and plate borders without changing text size, colour, position or animation timing. Three captures 250 ms apart retained stable readable edges without obvious shimmer. Headed ANGLE Metal checks passed at 1440x900, 1998x750, 2560x1080, 3425x1245, 1024x768, 900x1080 and 390x900 with a visible gold focus outline, zero console/page errors and zero horizontal overflow. A live 1300x900→3425x1245 resize retained the same hero canvas. At 1440x900 the scene rendered 100 frames in one second while visible, zero while offscreen or under a controlled hidden state, and 100 after resuming. A temporary forced renderer failure left the static SVG fallback visible with no hero canvas, error or overflow. `npm run lint`, `npm run build` and `git diff --check` passed.

### ~~Round 5.3: /engine-network wide-window headline anchor follow-up~~ ✅ COMPLETED

**Why this follow-up happened:** reviewing the round-5.2 state at the same ~3425x1245 window, the user approved the composition and the resolved gate/headline separation but felt the copy block now hugged the left edge on very wide windows. Round five's fixed 176 px cap holds the headline at ~5% of a 3425 px viewport while the gate stays pinned near 60.7%, opening an oversized dead gap between the h1 tail and the ring.

**What changed:** `src/app/engine-network/engine-network.css` only. The `.eng-hero-inner` left offset gained a third content-box term: `max(min((100% - 138rem) * 0.198, 12.22% - 11rem), 0rem)`. Below 2208 px content width the term is zero, preserving the validated round-five geometry everywhere it is tight — at 1998x750 the h1 tail already runs close to the ring's leftmost stroke, and an earlier flat `12.22%` draft of this change was measured re-introducing the gold-on-gold headline/halo collision there and was rejected in favour of the delayed ramp. Above 2208 px the copy walks right at 0.198 of the extra width, and the term caps at `12.22% - 11rem` so the block holds the approved 176/1440 relative anchor (~12.2% of content width) on ultrawide windows instead of pinning to absolute pixels. No scene, markup, scrim or typography change.

**Validation:** measured headline left offsets: 168.5 px at 1440x900 (canonical, unchanged under Chromium's classic scrollbar), 176 px at 1800x750, 1998x750 and 2208x900 (unchanged round-five values), 242.7 px at 2560x1080 (9.5% of content width) and 414 px at 3425x1245 (12.1%). Screenshots at 1998x750 across three animation phases match the round-five composition; 2560x1080 and 3425x1245 (three phases) show the rebalanced copy position with generous clear air to the ring, a legible kicker over passing documents, and the scrim pocket still pooling behind the copy. Compact rows 1024x768, 900x1080 and 390x900 computed identical pre-change padding (40.95/36/20 px) with unchanged layouts. A live 1300x900→3425x1245 sweep applied the offset monotonically (98.5 px → 176 px plateau → ramp → 409 px) with the same hero canvas and zero horizontal overflow throughout. Zero console/page errors at every viewport; the hero retained exactly one canvas per row. `npm run lint` exited 0 and `npm run build` statically exported all routes. Captures use the `r53b-*` prefix under `~/.dev-browser/tmp/`.

### Round six: canonical-page audit fixes

**Why and decisions:** Engine Network was selected as the canonical homepage presentation, so a source and headed-browser audit covered every animation phase, diagram and copy block. It now renders at production `/` without the fixed Design lab chip and remains at `/engine-network/` with the chip for internal comparison. The user required keeping all three evidence set-pieces and approved three other direction-setting decisions: flip the match diagram to the trace's AUD→INR direction, align the hero account cast even though that triggers the full WebGL matrix, and protect only the hero kicker with text-shadow. The implemented story is now pages → ledger → network → finding: the ledger's flagged A$9,500 withdrawal on 07 MAR 2024 and p. 214 becomes the first trace hop; A$9,400 follows on 09 MAR 2024; A$28,000 leaves through Wise on 02 APR; and ₹15,38,040 lands at HDFC on 04 APR at an exact FX 54.93.

**Implementation:**

- `LedgerPlate.tsx` and `engine-network.css`: enlarged the flagged rule by 0.75rem on desktop and the final browser-tuned 0.625rem on ≤700 px screens, matched the crimson wash and reserved a fixed 5 px after-note gutter so the rule sits exactly 4 px below the note. A shared single-cell inline grid keeps all three rotating statuses on one baseline while the longest final line sizes the caption in flow, and reconciliation waits until 5.6 s. The caption now reads `Extract 04 · rendered from cba_joint_8802_mar24.pdf and 41 companion statements`; row data and balances remain unchanged.
- `TraceDiagram.tsx` and `engine-network.css`: added route-side label placement plus desktop/mobile CSS-variable offsets without changing nodes, edges or the 15 s canvas cycle. Final desktop placements are CASH above, NAB −60 px x, HDFC above and WISE +20 px x. Final mobile placements are CBA +30 px x, NAB −60 px x/+12 px y, WISE +30 px x, HDFC −25 px x and ANZ hidden. Final note overrides are flagged −30/−34 px, A$28,000 −20/+14 px and FX −140/+62 px. The initial +40 px FX y offset still touched WISE on the phone proof and was browser-tuned to +62 px.
- `CurrencyMatch.tsx`, `engine-network.css` and `page.tsx`: preserved the 560×200 SVG, stations, motion path and 9 s cycle while changing only this route's content to NAB → WISE → HDFC. Renamed the amount/keyframe states to `out`/`in`, inserted a clean 0.5% swap gap, held the landed amount and MATCHED stamp together from 84–95%, widened the mobile chip and lifted station, sub, amount and verdict text sizes. The plate now uses the same 120 ms reveal delay as its sibling diagrams.
- `Scene.tsx` and `engine-network.css`: changed only baked account strings to CBA ··8802, NAB ··1130 and AMEX ··9010 (plus compact CBA ··8802), preserving all slot and texture geometry. Added a three-layer dark text-shadow to the hero kicker so passing paper cannot erase its gold glyphs.
- `page.tsx`: applied the canonical copy pass in British English, removed Oxford commas and schema/direction contradictions, and aligned the audience claim to fifty accounts. The production `/` render omits the fixed Design lab chip; `/engine-network/` retains it and returns to the gallery at `/internal-design/`.
- `engine-network.css`: added the ≤480 px 1.9rem/1.4-line-height proof-stat rule and tighter grid gap. The final 390/480 px value boxes measure 42.56 px and remain single-line throughout the counter animation; ≥481 px typography is unchanged.

**Validation:** headed ANGLE Metal verification passed for the hero at 1440x900, 1998x750, 2560x1080, 3425x1245, 1024x768, 900x1080 and 390x900 with one canvas, zero captured console/page errors and zero horizontal overflow. The same canvas survived a live 1300x900→3425x1245 resize. The scene produced zero frames offscreen and under a controlled hidden state, then resumed, while a temporary forced renderer failure left the static fallback visible with no canvas or page error. Ledger geometry/timing passed at 1440, 700 and 390; trace collision/containment passed at 1440, 1024 and 390 through a full reset; match phase sampling proved a clean swap and 0.99 s chip/verdict coexistence; proof and footer checks passed at their required widths. `npm run lint`, `npm run build`, the route-scoped dangling-token checks and `git diff --check` passed on the documentation-complete tree. Evidence uses `r6-root-*` under `~/.dev-browser/tmp/`.

### Round 6.1: current-year currency-rate update

**Why and decision:** the user replaced the illustrative 54.93 AUD→INR rate with 65.00 to better reflect the current-year exchange-rate range. The transfer direction, accounts and dates remain unchanged. The exact evidence-story arithmetic is now A$28,000 × 65.00 = ₹18,20,000.

**Implementation:** `CurrencyMatch.tsx` now shows WISE / FX 65.00 and HDFC / IN ₹18,20,000, including the travelling arrival chip and explanatory component comment. `TraceDiagram.tsx` uses the same ₹18,20,000 arrival annotation so the tracing and matching set-pieces remain one transaction. No animation timing, geometry or CSS changed.

**Validation:** `npm run lint` and `npm run build` passed. Headed browser checks at 1440x900 and 390x900 confirmed the trace and match show the same ₹18,20,000 arrival, the match shows FX 65.00, the full amount remains contained and legible throughout the animation, and the route has zero console errors, page errors or horizontal overflow. Evidence uses the `r61-fx65-*` prefix under `~/.dev-browser/tmp/`.

### Round seven: production site pages

**Why and decisions:** the user selected the core four production additions: About, Engagement & pricing, Contact and a branded 404. The About page tells the service story without people or invented history. Contact uses the established Bulma Root and Embeddings Formspree pattern. Public discovery expands through the footer and sitemap. The homepage originally retained its section-anchor navigation; the post-launch follow-up below supersedes that decision with one shared production header. The verbatim copy decks and behaviour contracts live in `documents/learnings/fintrace_site_pages_plan.md`.

**Implementation:**

- `SiteChrome.tsx` owns the shared sub-page header and production footer. The footer adds Home, About, Engagement & pricing and Contact; `site-pages.css` owns shared hero, heading, strip and section primitives.
- About and Engagement group each decorative mono-strip divider with the fact it introduces. The shared narrow layout therefore preserves the middle-dot rhythm without promoting punctuation into standalone rows at 390 px.
- `/about/` presents the grounded live-matter origin, service-not-software model and source-verifiability standard. `/engagement/` presents the four-stage engagement, the Excel ledger and findings-report deliverables and flat-fee-plus-per-page pricing shape. `/contact/` presents the six-field matter form and structured next steps. The root 404 carries the static constellation echo and return action.
- The rendered claims pass narrowed two About draft lines to the brief's closed claim set: removed the unverified “every engagement since” implication and replaced negotiation/tribunal outcome language with the approved human-verifiable, court-ready standard. The plan copy deck records the final strings.
- The homepage gold header, hero and engage actions target `/contact/`. The original footer extraction was the only home layout addition before the post-launch shared-navigation and footer-grid follow-up below.
- `ContactForm.tsx` posts `FormData` to Formspree form `xwvgoenw` with JSON acceptance, hidden subject/source/honeypot fields, native required controls, busy/status/alert semantics, reset on success and value preservation on failure.
- The contact form plate selector explicitly outranks the shared `section[id]` anchor rule so `#enquire` retains its planned 6rem scroll clearance instead of inheriting 4rem.
- The sitemap now contains exactly the four production URLs, superseding deployment-plan REQ-28's one-URL requirement. Internal design routes remain unlinked, excluded and noindexed.
- The contact error panel's restrained crimson is a recorded design exception to the flagged-evidence accent role. Text and `role="alert"` semantics carry the state independently of colour.

**Validation:** `npm run lint`, `npm run build`, static artefact assertions and `git diff --check` passed. Headed browser checks passed on `/`, `/about/`, `/engagement/`, `/contact/` and the branded unknown route at 1440x900 and 390x900 with zero application console/page errors and zero horizontal overflow. Copy/claims, CTA targets, reveal entrances, footer hit boxes, card hover, grid geometry, contact native validation, keyboard/focus order, forced-failure value preservation, hidden honeypot, 6rem anchor offset and responsive strip grouping passed scripted assertions. The hero href-only change passed the full seven-viewport 14-second matrix, same-canvas live resize, offscreen/hidden pause-resume and controlled renderer-failure fallback. Evidence uses `r7-*` under `~/.dev-browser/tmp/`. The multi-agent post-change audit reported two real CSS defects, both independently reproduced, fixed and reverified. Local fetch stubs verified the form’s success and failure state transitions. The real public form ID `xwvgoenw` ships, but a live HTTP 200 submission and dashboard receipt were deliberately not run by user decision; the production success path therefore remains unproven until the first real enquiry.

### Round eight: central metadata and social-share image

**Why and decisions:** production metadata previously duplicated page titles and descriptions and had no social-share image. The user selected a central `siteMetadata` plus `pageMetadata` pattern and one site-wide Open Graph image. The sub-page title separator was initially `/ FinTrace`, then superseded by the `| FinTrace` delimiter on 18 July 2026. Canonicals and sitemap URLs intentionally remain route-owned literals.

**Implementation:** `src/lib/metadata.ts` now owns the site name, default SEO title, canonical origin, locale, site description, social-image path and all four production page title/description pairs. The root layout consumes it for `metadataBase`, the `%s | FinTrace` title template and site-wide Open Graph/Twitter metadata. The home page uses an absolute title to preserve `FinTrace — Forensic financial analysis for legal matters`; About, Engagement & pricing and Contact inherit the pipe template. `public/images/og/fintrace-og.png` is a 1200×630 crawler-facing Engine Network composition whose visible text is limited to the wordmark, approved home-title clause and domain. Its settled homepage-scene diagram contains the live hero’s eight labelled account nodes, seven rim-connected edges, scanning gate and crimson CASH ATM hop.

**Validation:** `npm run lint`, `npm run build` and `git diff --check` passed after the pipe-delimiter revision. Built output preserves the absolute home and 404 titles and renders `About | FinTrace`, `Engagement & pricing | FinTrace` and `Contact | FinTrace`. Headed Chromium confirmed those three sub-page titles at 1440×900 and 390×900 with visible page headings, zero horizontal overflow and zero application console/page errors. All production outputs continue to carry the absolute Open Graph image, 1200×630 dimensions, alt text, site name and summary-large-image Twitter card.

### Round nine: shared production navigation, footer grid and Contact measure

**Why and decisions:** the post-launch review showed the homepage still carrying its historical Process / Ledger / Tracing / Proof navigation, the three footer blocks wrapping into a broken second row at desktop width and the Contact form providing only 461 px of usable field width. The user required one production navigation contract, a stable footer, no displayed mailbox address and a roomier Contact form based on the Embeddings contact page.

**Implementation:** `EngineNetworkPage.tsx` now renders `SiteHeader`, so the homepage and sub-pages expose the same About / Engagement / Contact links and assessment action. `SiteFooter` replaces the mailbox with a Contact-page action; `engine-network.css` uses a three-column desktop footer grid, two-column intermediate layout and one-column mobile layout. Every retained lab-route contact action also targets `/contact/` without changing its visual system. The Contact aside now carries only the three next steps and engagement scope; success and error states refer to the submitted work email or retry action. The contact container is capped at 80 rem, its desktop grid uses 4.5/7.5 proportions and its framed plate caps padding at 48 px. At 1440 px the resulting plate measures 710 px and its controls 612 px, closely matching Embeddings’ 592 px control measure; at 390 px the controls grow from 253 px to 285 px while preserving the engraved frame.

**Validation:** `npm run lint`, `npm run build` and `git diff --check` passed; the build exported all 21 static artefacts. A headed static-output sweep covered 15 changed routes at 1440×900 and 390×900 (30 combinations) with zero application console/page errors, zero horizontal overflow, zero mailbox text and zero direct-email links. The shared footer also passed its 1001/1000/701/700 px breakpoint checks with 44 px page-link targets. At 1440 px the Contact plate measured 710 px and its controls 612 px; at 390 px controls measured 285 px. Local fetch stubs verified sending, success, error, retry, field reset/preservation, honeypot geometry and `aria-busy`. The two-worker post-change audit found no implementation defect and two real stale-documentation defects; separate bug-fixer workers verified and corrected both, followed by the final lint and static export.

### Round ten: production completion - social image, brand icon set and deployment

**Why and decisions:** the final production pass closes the crawler-visible social-image gap, replaces the neutral design-lab favicon and reconciles code, documentation and the live site. The user confirmed that real public Formspree ID `xwvgoenw` must ship without a live test submission, that commit and push to `main` are part of this work, and that the favicon/logotype set is included while Safari/Firefox, formal accessibility review, analytics, notification-mailbox verification and lab-route housekeeping remain deferred.

**Implementation:** `public/images/og/fintrace-og.png` is the 1200×630 crawler-facing Engine Network composition. `src/app/icon.svg` is now a geometry-only luminous gate mark with no font dependency. `src/app/favicon.ico` contains PNG-compressed 16, 32 and 48 px FT monogram entries, while `src/app/apple-icon.png` is the 180×180 companion. Both raster icons derive from one 512×512 DPR-1 HTML render using Bricolage Grotesque and the locked obsidian, paper and gold palette. `AGENTS.md`, `DESIGN.md` and the implementation records now distinguish page visuals from crawler/browser-chrome assets and state the Formspree validation boundary exactly. The former README is absent following the repository’s documentation consolidation, so it carries no current-state claims to reconcile.

**Validation and deployment:** Node 22.23.1, `npm run lint`, `npm run build`, `git diff --check` and the DESIGN.md compatibility validator passed. Static assertions confirmed the 1200×630 social image is 291,784 bytes, the Apple icon is 180×180, the ICO has RGBA PNG payloads at 16/32/48 px, the SVG has no text or font reference, all icon/social links render, all five title outputs match, the sitemap has four locations, no internal-design link leaks and both recorded CSS regression selectors remain present. Headed Chromium verified `/`, `/about/`, `/engagement/`, `/contact/` and a branded 404 at 1440×900 and 390×900 with exact titles, both icon link types, the absolute social-image URL, zero horizontal overflow and zero application console/page errors. The expected 404 document diagnostic was excluded from application-error counts. Evidence uses `r8-*` under `~/.dev-browser/tmp/`. The two-worker audit found no asset/metadata defect and two real documentation defects; both were independently verified, fixed and revalidated. Implementation commit `9a0032235e2f10a1a141c568104728960490c9fa` deployed successfully through Pages run `29636640457`. Live production returned the expected four 200 page responses, identity asset content types, four sitemap locations and branded 404, while the live social PNG hash exactly matched the verified source. GitHub’s Node.js 20 action deprecation notices were non-blocking maintenance warnings.

### Round eleven: shared cross-product browser identity

**Why and decisions:** the user selected the approved FinTrace App Bricolage glow `F | T` mark as the active favicon for both FinTrace products. The change is limited to browser identity. The production wordmark, page titles, routes, social image, page copy and WebGL presentation remain unchanged. Both repositories retain their previous icon assets for rollback.

**Implementation:** `src/app/icon.svg` is byte-identical to the approved App vector and uses fixed Bricolage Grotesque outlines, equal letter-to-gate clearances and a clipped luminous gate. `src/app/favicon.ico` contains PNG-compressed 16, 32 and 48 px entries derived from that vector, and `src/app/apple-icon.png` is the 180×180 companion. The prior Root SVG, ICO and Apple icon are preserved byte-for-byte under `src/assets/browser-identity/`, which is not imported or emitted by the static build.

**Validation:** Node 22.23.1 `npm run lint` and `npm run build` passed. Source/export comparisons confirmed byte-identical SVG, ICO and Apple assets, the active App and Root SVG hashes match, and the archived Root hashes match the pre-change Git objects. Browser checks at 1440×900 and 390×900 confirmed the expected icon links, zero application console errors and zero horizontal overflow. The required two-agent audit found no runtime or asset defect; its only finding was stale wording in the FinTrace App implementation plan, which was independently verified and corrected.

---

## Current page inventory (route by route)

Sixteen user-facing routes are statically exportable: production `/`, `/about/`, `/engagement/` and `/contact/`; `/internal-design/`; six original concepts including the untouched `/engine` reference; and five dark Evidence Engine variations. A branded root 404 completes the production shell. Engine Network is the flagship presentation shared by production `/` and internal comparison route `/engine-network/`, with its wide-aspect rebalance completed in round five and its full audit-fix pass completed in round six.

### Internal gallery `/internal-design/` (`src/app/internal-design/page.tsx` + `internal-design.css`)

Deliberately neutral so it never biases comparison: Newsreader (editorial serif) + Spline Sans Mono on near-black, staggered `lab-rise` entrance animation. Two registers: `DESIGNS` (the six round-one concepts, numbered 01-06 with palette swatch dots and voice lines) and `ENGINE_VARIATIONS` (V1 Refined, V2 Trace, V3 Ledger, V4 Network, V5 Flow - all Dark - under the heading "The Evidence Engine - variations", label "Rounds two & three · base kept as reference"). All hrefs use trailing slashes (`/engine-refined/` etc.).

### The six round-one concepts (kept as historical candidates)

1. `/ledger` - "The Ledger". Light editorial broadsheet: ivory paper, Fraunces display serif, ruled ledger lines, oxide-red accents; hero is a self-reconciling ledger table (rows enter, category chips stamp, a $9,500 ATM withdrawal flags; balances genuinely reconcile to 13,098.05). Voice: "Every dollar, accounted for."
2. `/trace` - "Follow the Money". Dark ink-navy investigative evidence board: canvas account-network hero with a red thread drawing through it (`TraceNetwork.tsx`), and the original cross-currency set-piece (`MoneyFlow.tsx` - NOTE: still has the old overlapping MATCHED/chip geometry; only the engine variations received the fix). Archivo + IBM Plex Mono. Voice: "Money leaves a trail."
3. `/clarity` - "From Chaos to Clarity". Swiss white + cobalt grid: strewn scanned pages snap into a live spreadsheet (replayable), 50-vs-10-hour proportional bars. Familjen Grotesk + Spline Sans Mono.
4. `/engine` - "The Evidence Engine", THE UNTOUCHED ROUND-ONE REFERENCE. Documents stream into the golden gate and exit as the original ~950-point burst/lattice ("dust") - kept exactly as round one shipped it so variations can be judged against it. Everything below the hero is DOM-only and scroll-choreographed.
5. `/exhibit` - "The Exhibit". Brutalist court exhibit: stamped headline with ink bleed, EXHIBIT FT-01 stamp, typed matter register, finding cards that flip to their source excerpts, footnote source register. IBM Plex Mono + Archivo Black, white/black/stamp red.
6. `/chambers` - "Chambers". Quiet luxury: letterpress reveal, twin gold hairlines, wax-seal FT monogram, parchment invitation CTA. Cormorant Garamond + EB Garamond + Cinzel on deep green.

### The Evidence Engine family - shared system (all five variations + base)

**Palette (CSS vars in each route's stylesheet):** obsidian background `#0d0b09`; gold `--gold #d4a94e`; bright gold `--gold-bright #f0d491`; warm paper/off-white text `--paper`; muted `--grey-warm`; restrained crimson for flags - `#b3231f` (threads/edges) and `#e0503a` (text/row glints). Fog colour matches the background so geometry materialises/dissolves without pop-in.

**Typography (`fonts.ts`, identical in every engine route):** Bricolage Grotesque (variable, `--font-eng-display`) for display/headlines; Fragment Mono (400, `--font-eng-mono`) for kickers, labels, stats, buttons. This pairing is user-locked - do not change it.

**Wordmark:** "Fin | Trace" - `Fin` in paper, a luminous vertical gate bar, `Trace` in gold (`.eng-wordmark`); reused in header and footer, linking to the route's own root.

**Page skeleton (every engine-family page, in order):**

1. Fixed header: wordmark + nav (Process / Capabilities / Proof - engine-trace adds Tracing) + gold "Request assessment" button. Every candidate preserves its historical section navigation while routing the assessment action to `/contact/`.
2. `<Hero />` (client component): layered bottom-to-top as (a) designed static SVG fallback (instant first paint, echoes that route's scene), (b) the WebGL scene layer that cross-fades in when its first frame renders (`.eng-scene-layer.is-ready`, `onReady` callback), (c) a contrast scrim, (d) DOM headline block lower-left (kicker "Forensic infrastructure for legal teams", h1 "The evidence engine.", lede, two CTAs), (e) golden scroll-cue filament, (f) mono stat strip along the hero's bottom edge ("Thousands of pages | ≈50 accounts | 15 years of statements | One ledger"). The flagship network scrim has three layers (headline pocket + bottom-up + left-in), and its headline's content-width left offset holds 176 px from 1440 px up to 2208 px content width, then walks right at 0.198 of the extra width, capped at the approved ~12.2% relative anchor (`12.22% - 11rem`), so ultrawide windows keep the canonical composition without the copy hugging the left edge or re-meeting the gate (round 5.3).
3. `#process` - "Four stages. One chain of evidence." Four stages with a drawing progress line: 01 Intake (any bank/order/era, no pre-sorting), 02 Extraction (one structured Excel ledger), 03 Analysis (cash patterns, gambling/crypto, related-account transfers, cross-currency matches), 04 Findings (report where every finding cites its exact source page).
4. Variant-specific set-piece section(s) - see per-route notes below (`#match` everywhere; `#tracing` on trace; `#ledger` on ledger; the flagship network carries ALL THREE as a story since round four).
5. `#capabilities` - "Built to withstand scrutiny." Six spec cards: Universal intake, Structured ledger, Auto-categorisation, Anomaly detection, Cross-account tracing, Source-linked findings (each tagged Input/Output/Analysis/Report).
6. `#proof` - "Fifty hours of review. Delivered in about ten." Animated counters (50→10 hrs backwards meter, ≈50 accounts, 15 yrs, 1,000s of pages) + the note that findings closely matched the instructing lawyer's independent analysis.
7. `#for` - "For matters that hinge on the money." Five audience cards, family-law property matters featured as "The proven wedge" (plus public trustees/government legal, forensic accountants, insolvency practitioners, estate & financial-abuse matters).
8. `#engage` - CTA plate with sheen sweep: service positioning (flat engagement fee + per-page pricing), gold "Request a matter assessment" CTA + ghost Contact-page action. Every candidate routes both actions to `/contact/` without changing its visual system.
9. Footer (wordmark, "Forensic financial analysis for the legal profession.", Contact-page action, "Engaged per matter · Australia-wide", © line). Evidence Engine comparison routes retain a fixed "Design lab" back-chip to `/internal-design/`; the production `/` render intentionally removes it.

**Shared WebGL scene bones (every engine `Scene.tsx`):** three.js dynamically imported client-side only; renderer in try/catch (missing WebGL → static fallback survives); DPR ≤ 2; fog `#0d0b09` 9→24; camera fov 34 at (0, 0.9, 10.5 desktop / 14 mobile) with gentle drift + pointer parallax, lookAt(0, 0.1, 0); incoming paper documents (instanced planes with a baked statement CanvasTexture) streaming from x −13 into the gate, funnelling and shrinking as they're swallowed; two gold tori + two additive glow sprites standing in for bloom; ambient gold dust; IntersectionObserver + visibilitychange rAF gating; ResizeObserver-driven camera/renderer resize; exhaustive disposal; and delta clamped to [0, 0.05]. The original variants keep stage x +1.1 wide / −0.7 compact and a gate at y 0 with radii 1.75/1.58. The flagship network now differs before the post-gate output as well: its wide stage shift is aspect-anchored, its gate is y 0.4 with radii 1.45/1.31, its glow is smaller and upward-biased, and its paper funnel starts at 0.72.

**The shared cross-currency match set-piece (`CurrencyMatch.tsx` per route + `ecm-*` CSS block):** the /trace "CROSS-CURRENCY MATCH - RECONSTRUCTED" diagram redrawn in engine gold, added in round three to ALL five variation pages. Server component; pure-CSS animation armed by the route's `Reveal` (`.eng-reveal.is-visible`), motion gated behind `@supports (offset-path: …)` so non-supporting browsers keep the static diagram. Fixed shared geometry: viewBox `0 0 560 200`; route path `M 76 118 Q 178 52 280 118 Q 382 52 484 118`; node y 118, names y 144, subs y 162; travelling chip in the y 78–102 band; MATCHED plate in the y 40–64 band, preserving 14 units of air. Refined, trace, ledger and flow keep the original HDFC INR→WISE→ANZ AUD content and their route-prefixed animation copies. Canonical `/engine-network` intentionally diverges in content only: x 76 is NAB ****1130 / OUT A$28,000 · 02 APR, x 280 is WISE / FX 65.00 and x 484 is HDFC ****3321 / IN ₹18,20,000 · 04 APR. Its 9 s `ecmnet-*` loop uses a non-overlapping amount swap and holds the landed chip with MATCHED for 0.99 s; ≤767 px rules enlarge the SVG text and chip. Keyframe prefixes remain `ecm-` (trace), `ecmr-` (refined), `eclm-` (ledger), `ecmnet-` (network) and `ecmf-` (flow).

**Per-route files:** every engine route contains `page.tsx`, `<route>.css`, `fonts.ts`, `Hero.tsx`, `Scene.tsx`, `Reveal.tsx` (IntersectionObserver reveal wrapper adding `is-visible`), `Stat.tsx` (count-up/count-down number animation), and - variations only - `CurrencyMatch.tsx`; engine-trace additionally `TraceDiagram.tsx`; engine-ledger additionally `LedgerPlate.tsx`; engine-network (the flagship) additionally carries its own copies of BOTH `LedgerPlate.tsx` and `TraceDiagram.tsx` (round-four consolidation — identical markup, CSS rescoped under `.dsn-engine-network`, ledger keyframes re-prefixed `engnet-lt-*`).

### V1 `/engine-refined` - the control (calm record band)

Hero output: exactly 9 (desktop) / 5 (mobile) legible ledger-row slabs - baked 512x64 CanvasTextures of real transactions (Woolworths groceries −214.63, flagged ATM −9,500.00 CASH, Wise AUD→INR −5,200.00 CROSS-CCY, salary +8,412.90 INCOME) on gold-framed obsidian plates - born at the gate, overshoot-locking (ease-out-back) into a SINGLE-depth-layer, 3-row lattice (row gap 0.62) and drifting right into the fog. Evenly phase-spaced (i/SLAB_COUNT), scale 1.15/0.9, scatter ±0.5 y / ±0.25 z, speed ~0.045; DOC_COUNT 12/7, DUST 110/60 at opacity 0.24. This is the round-two control with the round-three "way too busy" feedback applied: a handful of large readable records instead of a 28-slab swarm. Everything else is the base page + better-ui polish. Sections: process → match → capabilities → proof → for → engage.

### V2 `/engine-trace` - the docked evidence network

Hero output: evidence cards (gold-edged citation plates baked as textures - "FT-0114 / p. 214 · VERIFIED" with data rules) emerge through the gate every 1.6 s, arc-fly ~2.2 s and overshoot-dock into a fixed branching tree: desktop root (1.7, 0, 0), branches (2.45, ±1.05, ∓0.4/0.35), four leaves x 3.15-3.45 / y ±0.62 and ±1.8 / z −0.25..−0.9, CARD_SCALE 0.92; mobile 5 slots (root 0.4, leaves ≤1.95, scale 0.6). Connectors join card BORDER midpoints (±CARD_W/2 × scale - lines never cross a card face; this is the fix for "a string with cards connected to it") and each draws in over 0.6 s only after BOTH endpoint cards dock; one leaf edge is crimson with a warming leaf card. 26 s cycle: build (complete ~12.4 s desktop / 9.2 s mobile) → dwell with 3 gold pulse motes hopping edges (1.9 s per hop) + gentle group sway → the whole constellation drifts +4.5 x and fades into fog → rebuild. Hero fallback SVG echoes the branching network. Extra section `#tracing` (nav link "Tracing", placed after `#process`): "Fifty accounts. One thread of evidence." - the DOM/SVG account-network diagram (`TraceDiagram.tsx`, gold, crimson only on the flagged CASH hop) + the CurrencyMatch set-piece. Sections: process → tracing (network diagram + match) → capabilities → proof → for → engage.

### V3 `/engine-ledger` - the live spreadsheet (hero reworked FRONT-ON + interactive in round four)

Hero output: ONE spreadsheet sheet standing FRONT-ON to the camera (round-four feedback: the old +1.0 rad yaw made it hard to read; resting yaw is now −0.04 rad with −0.03 pitch — a whisper of angle so it never reads as a flat overlay). A 3.1-unit-wide plane (wide-layout centre stage-local (1.66, 1.08, −0.45) — riding HIGH so the DOM h1 never crosses it, left edge tucked into the gate's halo; compact scale 0.72 at (0.9, 2.75, −0.2), parked in the clear band between the fixed header and the kicker; layout chosen by aspect — `compact = width < 768 || aspect < 1.2` — and for aspects 1.2–1.6 a continuous fit shrinks the sheet and eases it toward the gate, floored at 0.55, re-applied live on resize) carrying a baked 1120x456 canvas that is a literal spreadsheet: header band DATE | DESCRIPTION | DEBIT | CREDIT | CATEGORY, `#` row-number gutter (1-8), gold hairline gridlines, alternating row shading, outlined category chips, and eight grounded rows - Woolworths/groceries, salary/income, ATM WITHDRAWAL - CROWS NEST −9,500.00 CASH rendered fully in crimson `#e0503a` with a row wash, BP fuel, internal transfer to J SACINO ****4417, Wise AUD→INR cross-ccy, Sportsbet/gambling, rent received. 13.2 s cycle synced to the document stream: fade-in on headers-only, rows land at 0.9 s + n×1.1 s each with a bright entry band + left tick that clears a beat later, dwell, fade out, invisible reset. HOVER (round four, desktop pointer): window-pointermove raycast against the sheet quad — the sheet tips toward the pointer like a held card (±0.05 rad), lifts 0.12 toward the camera, the frame brightens, an additive gold aura breathes in behind the plate, the row under the cursor takes a gold wash + left tick with lifted ink, and the sheet's OWN clock holds at the brink of fade-out while hovered so the finished ledger stays up for inspection. The canvas redraws ONLY on state changes (row landings, hover transitions), never per frame. Extra section `#ledger` (after `#process`): "Every line entered. Every line sourced." - the DOM reconciling statement table on an obsidian machined plate (`LedgerPlate.tsx`; same verified row data as round one, balances reconcile; flag rule drawn as one continuous `pathLength={1}` dash draw with `vector-effect: non-scaling-stroke`, making the round-one fragmentation defect impossible). Sections: process → ledger → match → capabilities → proof → for → engage.

### V4 `/engine-network` - the account constellation - THE FLAGSHIP (round-four default; round-five wide rebalance)

Hero output: an account-network constellation - the /trace hero network reimagined in engine gold. Eight nodes wide / five compact, each a thin gold torus ring + 128x128-baked soft dot core + 512x128-baked mono label plate. The label textures bypass mipmaps and use direct linear sampling so their approximately 13-18 px projected glyphs keep clear caps, baselines and plate borders. The canonical account cast is ANZ ··4417, WISE, CBA ··8802, flagged CASH ATM, AMEX ··9010, HDFC ··3321, NAB ··1130 and BTC WALLET; compact retains ANZ, WISE, CBA, CASH and HDFC. Node slots and rim-inset edges remain the round-four/4.1 design: ANZ hub (2.35, 0.05, −0.2); WISE (1.7, 1.5, −0.55); CBA (3.3, 1.15, −0.9); flagged CASH ATM (2.6, −1.6, 0); AMEX (1.45, −1.3, −0.35); HDFC (2.35, 2.55, −0.9); NAB (3.4, 2.0, −1.5); BTC (3.45, −0.55, −1.1). Per-node `labelOffset` values keep every plate on its thread-free side. Wide framing uses stage x `0.6875 × aspect` and fit `clamp((2.5225a − 0.35) / 3.686, 0, 1.3)`, with vertical slot compression to 0.95 at the cap, constant authored world depth through a `1 / fit` z-position ratio, and node-core local scaling of `1 / sqrt(fit)`; compact remains the five-node round-4.1 layout. The gate is y 0.4, radii 1.45/1.31, with a 512x512-baked scale-5.5 halo biased +0.55 and a scale-2 core biased +0.15. Warm papers funnel from progress 0.72 and gold-tint near entry. The continuously animated document mesh bypasses stale frustum bounds and dispatches its InstancedMesh disposal event during teardown. The 28 s cycle and +3.4 exit remain unchanged, but births carry pooled mote trains, settled threads carry sliding dashes, the flagged hop lands a ring flare + underline, document swallows flash the core, hero `engine.` carries the 3.9 s sheen and the kicker carries a dark shadow against passing paper. Responsive wide and five-node compact fallback SVGs echo the matching lifted gate projection before WebGL appears or whenever it is unavailable.

BELOW THE FOLD (round-four consolidation, audited and unified in round six): `#ledger` "The evidence, structured" uses the CBA March 2024 extract and widened flag rule; `#tracing` "The evidence, connected" follows the same 07 MAR withdrawal through the final 04 APR FX match with collision-free route-side labels; `#match` "The evidence, matched" enlarges that exact A$28,000→₹18,20,000 Wise hop at FX 65.00. Linking ledes carry the arc pages → ledger → network → finding. The shared SiteHeader now exposes About / Engagement / Contact on both `/` and `/engine-network/`; sections remain process → ledger → tracing → match → capabilities → proof → for → engage. The shared production `/` render omits the fixed lab chip, `/engine-network/` retains it for internal comparison, and phone proof stats use the round-six ≤480 px single-line rule.

### V5 `/engine-flow` - the money trail (NEW in round three, replaces `/engine-light`)

Hero output: three static quadratic-bezier money trails leaving a shared start just right of the gate (1.2, 0, 0), fanning VERTICALLY to ringed, mono-labelled destinations - ANZ ··4417 end (3.0, 1.7, −0.5), WISE end (3.35, 0.12, −1.1), CASH ATM end (2.7, −1.7, 0.15) - drawn as dashed gold lines (~64 samples, opacity ~0.35, out-of-phase shimmer) with mid waypoint rings at ~t 0.42. Amount pulses (a lead comet sprite + 3 trailing sprites at decreasing opacity/scale; 12 sprites total off one shared dot texture) travel the curves on staggered clocks - 4.6 s, 5.6 s (+1.9 s offset), 5.1 s (+3.4 s offset) - swelling as they cross waypoints; every arrival at CASH ATM lerps its ring gold→crimson `#b3231f` with a brief crimson glow flash, then cools. Curves are never rebuilt per frame - life comes from pulses, shimmer and camera drift. Mobile: fan compressed (spreadX 0.42, spreadY 0.66, fixtures 0.62 scale, +0.35 y bias), stage shift −0.7. Fallback SVG echoes three dotted diverging routes with rings. Sections: process → match → capabilities → proof → for → engage.

---

## Repo state (current)

- Branch `main`, tracking `origin/main` in public repository `Culpable/fintrace-root`. Commit `85a1cac` promoted Engine Network and isolated the internal lab; `13bf3f2` added the native Pages workflow. Push-triggered run `29573382952` and post-DNS workflow-dispatch run `29583384962` both completed install, lint, build, artefact upload, and deployment successfully without a deployment secret or `gh-pages` branch.
- Production completion commit `9a0032235e2f10a1a141c568104728960490c9fa` added the site pages, central metadata, crawler/browser identity assets and reconciled records. Push-triggered Pages run `29636640457` completed successfully, and the live page, asset, sitemap and 404 assertions passed on 18 July 2026.
- Repository roles were finalised on 17 July 2026 without changing this lab’s design scope. The public design lab now lives at `/Users/sacino/fintrace-root` and `Culpable/fintrace-root`; the private application now lives at `/Users/sacino/fintrace` and `Culpable/fintrace`. The application’s visible product identity is FinTrace, while this repository remains the internal brand-selection site.
- The application continues on the unchanged Vercel project ID under the public production alias `fintrace-red.vercel.app`, with `statement-analysis-red.vercel.app` retained as a rollback alias. This front-facing repository deploys separately to GitHub Pages at `fintrace.com.au` and does not share the application runtime.
- Historical references to the former repository paths remain valid only where they document completed design rounds or migration evidence. New operational references must use the current paths above. Product claims remain grounded in `/Users/sacino/fintrace/documents/reference/brand_naming_background.md`.
- Static build includes the production root, internal gallery, original concepts, engine reference, five variations, robots, sitemap, not-found page and browser identity routes. Every route remains exportable.
- The former README and superseded planning files were removed during documentation consolidation. This file is the current implementation-history source of truth; `AGENTS.md` carries the binding rules.
- Verification screenshots are transient under `~/.dev-browser/tmp/`: live Pages evidence is `ft-pages-live-desktop-root.png` and `ft-pages-live-mobile-root.png`; local deployment-matrix evidence uses `ft-pages-{desktop,mobile}-{root,engine-network,internal-design}.png`; round-six implementation evidence uses `r6-root-*` and the isolated pre-fix reproduction uses `r6-pre-*`.

## Known open items / caveats (worth a new contributor's attention)

- Aspect adaptation now diverges further: the network owns aspect-anchored stage shift, expandable fit and wide-short vertical slot compression; ledger owns only the round-4.1 compact-by-aspect/continuous-fit system; `/engine-refined`, `/engine-trace` and `/engine-flow` still assume fixed +1.1 wide shift and width-first layouts. Port the flagship framing only if another variation advances.
- Crossing the wide↔compact threshold on a live window resize does not rebuild the scene (layout is chosen at mount; only the in-layout fit is applied live). A reload lands the correct layout. Pre-existing behaviour, unchanged in round 4.1.
- `/trace`'s own `MoneyFlow.tsx` still has the ORIGINAL overlapping MATCHED/chip geometry - the fix was applied only to the five engine variations (per the user's instruction scope). If /trace ever advances, port the canonical geometry.
- All evidence cards in the /engine-trace hero share a single baked "FT-0114" texture; varying citation numbers per card would deepen the illusion (cheap: bake 3-4 textures round-robin).
- All four non-canonical variations still carry the pre-flip INR→AUD match content, while `/engine-trace` also retains the 2021 trace dates. These are historical comparison routes; if any is promoted, port the canonical network story and choreography deliberately rather than changing them piecemeal.
- No automated tests exist; every change re-runs lint, build and headed browser checks. Network scene changes require the seven-viewport matrix plus resize/fallback/lifecycle checks; other UI work retains the project matrix. Browser checks are Chromium-only so far.
- The base `/engine` intentionally still shows the round-one point-burst "dust" - it is the comparison reference and must NOT be "fixed".
- Analytics, Safari/Firefox verification, notification-mailbox delivery and the optional formal accessibility review remain open. Chromium focus visibility is verified, but complete keyboard flows and rendered contrast pairs are not formally verified.
- Round-5.1 resolved the high-fit node softness with 2x dot/glow texture bakes, `1 / sqrt(fit)` node-core compensation and `1 / fit` z-position compensation. Frame-edge perspective stretch remains inherent at ~85-90% viewport width, but the 3425x1245 proof shows crisp hot centres and clear crown spacing without raising the fit cap.
- Round-5.2 removed the label texture's mipmap prefiltering, which was the remaining source of horizontal glyph-edge softness at the user's 3425x1245 review viewport. The labels remain raster CanvasTextures, so SDF/MSDF text or DOM overlays would be the next architectural option only if future review requires resolution-independent typography.

## Next steps

1. **Housekeeping decisions now the flagship is mature:** decide whether refined/trace/ledger/flow stay for comparison or retire, whether the gallery labels V4 as the default, whether the six round-one concepts remain listed, and whether any flagship framing should be ported to another route.
2. **Remaining production work:** decide analytics, verify Safari/Firefox and notification-mailbox delivery, and run any optional accessibility review within the workspace's no-reduced-motion constraint.
3. **If any further network scene work happens:** obey the round-five/5.1/5.3 formulas (including the headline offset's 2208 px ramp onset and ~12.2% cap — its clear-air budget at 1998x750 has no slack), preserve the max-sway margin, and rerun 1440x900, 1998x750, 2560x1080, 3425x1245, 1024x768, 900x1080 and 390x900 plus resize/fallback/lifecycle checks. Frustum maths sets the budget; projected browser evidence proves it.
