---
/**
 * Ordered registry for provider-owned snippets that must appear in <head>.
 * Import provider wrapper components above and render them below in exact source order.
 * Keep consent defaults before tags and base tags before dependent page conversions.
 */
---

<!-- Render imported sitewide head components here in required order. -->
