{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Astro: pnpm dev (4332 development)",
      "type": "node-terminal",
      "request": "launch",
      "command": "corepack pnpm --dir site dev",
      "cwd": "${workspaceFolder}",
      "env": {
        "NODE_ENV": "development",
        "PORT": "4332"
      }
    }
  ]
}
